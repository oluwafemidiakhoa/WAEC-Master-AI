# Firebase Configuration
# Copy this file to .env and add your actual Firebase values
# NEVER commit .env files to version control!

# Firebase Project Settings
FIREBASE_API_KEY=your_firebase_api_key_here
FIREBASE_AUTH_DOMAIN=your_project.firebaseapp.com
FIREBASE_PROJECT_ID=your_project_id
FIREBASE_STORAGE_BUCKET=your_project.appspot.com
FIREBASE_MESSAGING_SENDER_ID=your_sender_id
FIREBASE_APP_ID=your_app_id
FIREBASE_MEASUREMENT_ID=G-your_measurement_id

# Environment
NODE_ENV=development

# Security Notes:
# - Firebase client SDK keys are safe to expose in client-side code
# - However, proper Firestore security rules are CRITICAL
# - Always validate data server-side in Firebase Functions if possible
# - Use Firebase App Check for additional production security